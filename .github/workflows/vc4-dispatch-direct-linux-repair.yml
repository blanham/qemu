name: Dispatch repaired VC4 direct-Linux control

on:
  push:
    branches:
      - agent/vc4-hetero-direct-linux
    paths:
      - .github/workflows/vc4-dispatch-direct-linux-repair.yml
  workflow_dispatch:

permissions:
  actions: write
  contents: write

jobs:
  dispatch:
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/vc4-hetero-direct-linux
          fetch-depth: 0

      - name: Dispatch the repaired control workflow
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import os
          import urllib.request

          repo = os.environ['REPOSITORY']
          request = urllib.request.Request(
              f'https://api.github.com/repos/{repo}/actions/workflows/'
              'vc4-hetero-direct-linux.yml/dispatches',
              data=json.dumps({
                  'ref': 'agent/vc4-hetero-direct-linux',
              }).encode('utf-8'),
              headers={
                  'Accept': 'application/vnd.github+json',
                  'Authorization': f"Bearer {os.environ['GH_TOKEN']}",
                  'Content-Type': 'application/json',
                  'X-GitHub-Api-Version': '2022-11-28',
                  'User-Agent': 'vc4-direct-linux-dispatch',
              },
              method='POST',
          )
          with urllib.request.urlopen(request, timeout=60) as response:
              if response.status != 204:
                  raise SystemExit(f'unexpected dispatch status {response.status}')
          PY

      - name: Remove one-shot dispatcher
        run: |
          set -euo pipefail
          git config user.name "VC4 CI"
          git config user.email "vc4-ci@users.noreply.github.com"
          git rm -- .github/workflows/vc4-dispatch-direct-linux-repair.yml
          git commit -m "ci/vc4: clean direct-Linux dispatcher [skip ci]"
          git pull --rebase origin agent/vc4-hetero-direct-linux
          git push origin HEAD:agent/vc4-hetero-direct-linux
