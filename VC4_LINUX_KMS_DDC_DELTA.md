# VC4 Linux KMS HDMI DDC delta

DDC advances the frontier: **false**

- Marker score before: `0`
- Marker score after: `0`
- Bound components before: `1`
- Bound components after: `1`

The hardware change is retained only when this controlled two-boot comparison advances a native-KMS marker, binds an additional VC4 component, or clears an explicit missing-DDC supplier failure.
